/// Screen breakpoints (logical pixels).
abstract final class Breakpoints {
  /// Reference design width used for [sp] scaling.
  static const double referenceWidth = 390.0;

  static const double phoneMax = 600.0;
  static const double tabletMax = 1024.0;
}
